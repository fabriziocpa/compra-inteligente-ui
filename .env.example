# Base URL of the Compra Inteligente backend API.
# The BFF (Next.js route handlers) reads this server-side ONLY — it is never
# exposed to the browser, so do NOT prefix it with NEXT_PUBLIC_.
API_BASE_URL=http://localhost:8000/api/v1
